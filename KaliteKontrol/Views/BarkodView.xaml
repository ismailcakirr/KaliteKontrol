<UserControl
    x:Class="KaliteKontrol.Views.BarkodView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:KaliteKontrol.Viewmodels"
    xmlns:views="clr-namespace:KaliteKontrol.Views"
    d:DataContext="{d:DesignInstance Type=viewmodels:BarkodViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Viewbox Margin="50">
                <Grid>
                    <Border
                        Padding="50"
                        BorderBrush="{Binding BorderColor}"
                        BorderThickness="15"
                        Visibility="{Binding MessageVisibility}">
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="44"
                            FontWeight="Bold"
                            Text="{Binding Mesaj}"
                            TextWrapping="Wrap" />
                    </Border>
                    <StackPanel Margin="50" Visibility="{Binding ProgressVisibility}">
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="22"
                            FontWeight="Bold"
                            Text="Database kontrol ediliyor..."
                            TextWrapping="Wrap" />
                        <ProgressBar IsIndeterminate="{Binding DbProgress}" Value="0" />
                    </StackPanel>
                </Grid>
            </Viewbox>
        </Grid>
    </Grid>
</UserControl>
